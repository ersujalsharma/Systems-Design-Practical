package Player;

public class Player {
    public String id;
    public int currentPos;
    public Player(String id, int currentPos) {
        this.id = id;
        this.currentPos = currentPos;
    }
}
